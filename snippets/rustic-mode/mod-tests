# -*- mode: snippet -*-
# name: mod-test
# key: mod-test
# --

pub fn it(a: Vec<i32>, k: i32) -> i32 {
  $0todo!()
}

#[cfg(test)]
mod tests {

    use super::*;

    #[test]
    fn test_it() {
        let a = vec![];
        let k = 2;
        let answer = 5;
        assert_eq!(answer, it(a, k));
    }
}