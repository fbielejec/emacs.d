# -*- mode: snippet -*-
# name: sliding_window_fixed_k
# key: <fixed_sliding_window
# --

fn $0fixed_k_window(a: Vec<i32>, k: i32) -> i32 {
    let k = k as usize;

    // current tracks the window
    let mut current = 0;

    // build the first window
    for i in 0..k {
    }

    // update the answer    

    // keep moving the window
    for i in k..a.len() {
        // Add arr[i] to window
        // Remove arr[i - k] from window
        // Update the answer
    }

    // return the answer
    todo!()
}
