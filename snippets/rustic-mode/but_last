# -*- mode: snippet -*-
# name: but_last
# key: <but_last
# --

// skip(1) for but-first
for (i, current) in a.iter().enumerate().skip(1).take(a.len() - 2) {
    let previous = a[i - 1];
    println!("i: {i} PREV: {previous}: CURR: {current}");    
}