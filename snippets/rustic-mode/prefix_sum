# -*- mode: snippet -*-
# name: prefix_sum
# key: <prefix_sum
# --

let prefix: Vec<i32> = a$0
    .iter()
    .scan(0, |sum, element| {
        *sum += element;
        Some(*sum)
    })
    .collect();

let sum = |i: usize, j: usize| -> i32 { prefix[j] - prefix[i] + a[i] };
